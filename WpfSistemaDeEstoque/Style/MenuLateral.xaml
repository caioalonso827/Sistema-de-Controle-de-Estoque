<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">


    <Style TargetType="{x:Type RadioButton}" x:Key="MenuLateral">
        <Style.Setters>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type RadioButton}">
                        <Border Background="{TemplateBinding Background}" CornerRadius="10">
                            <Grid Background="Transparent" >
                            <TextBlock Text="{TemplateBinding Content}" FontSize="25" Foreground="White" FontWeight="SemiBold" HorizontalAlignment="Left" Margin="20,0,0,0"></TextBlock>
                        </Grid>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>

            <Setter Property="Background" Value="Transparent"></Setter>
        </Style.Setters>

        <Style.Triggers>
            <Trigger Property="IsChecked" Value="True">
                <Setter Property="Background" Value="#153657"></Setter>
            </Trigger>
        </Style.Triggers>
    </Style>
</ResourceDictionary>